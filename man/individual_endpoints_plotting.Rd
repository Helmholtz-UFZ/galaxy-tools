% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_WIP_double_var_individual_endpoints_func.R,
%   R/07_individual_endpoints_func.R
\name{individual_endpoints_plotting}
\alias{individual_endpoints_plotting}
\title{Plot timeseries for individual endpoints (WIP)}
\usage{
individual_endpoints_plotting(
  control_var1,
  unit_var1,
  endp_groups,
  habituation_assay_list,
  reference_pos,
  exp_name,
  pathToExp
)

individual_endpoints_plotting(
  control_var1,
  unit_var1,
  endp_groups,
  habituation_assay_list,
  reference_pos,
  exp_name,
  pathToExp
)
}
\arguments{
\item{control_var1}{Control value for `var1` used to include the control series in plots.}

\item{unit_var1}{Character string. Unit of measurement for variable 1 (var1). If var1 has no unit, set `unit_var1 = NA`.}

\item{endp_groups}{Named list of endpoint groups to plot together, e.g. list(group1 = c("BSL1","BSL2"), group2 = c("VMR1","VMR2")).}

\item{habituation_assay_list}{Character vector of assays for which to generate habituation plots (e.g., `c("ASH1")`).}

\item{reference_pos}{Numeric y-position for drawing acoustic stimuli reference lines/boxes.}

\item{exp_name}{Character string specifying the experiment name, used in input and output file naming.}

\item{pathToExp}{Character string. Path to the experiment directory containing necessary subdirectories for input and output.}
}
\description{
Creates timeseries plots per endpoint, chemical, and experiment using median
values and MAD as error. Optionally groups endpoints and generates
habituation plots (e.g., ASH1 vs stimulus number).

Creates timeseries plots per endpoint, chemical, and experiment using median
values and MAD as error. Optionally groups endpoints and generates
habituation plots (e.g., ASH1 vs stimulus number).
}
\details{
Required global objects loaded via aesthetics: `assay_data`, `acoustic_stimuli`,
and `stimuli_classes`.

Required global objects loaded via aesthetics: `assay_data`, `acoustic_stimuli`,
and `stimuli_classes`.
}
\examples{
\dontrun{
individual_endpoints_plotting(
  control_var1 = 0,
  unit_var1 = "\u00B5M",
  endp_groups = list(group1 = c("BSL1","BSL2")),
  habituation_assay_list = c("ASH1"),
  reference_pos = -1,
  exp_name = "my_experiment",
  pathToExp = "/path/to/experiment"
)
}

\dontrun{
individual_endpoints_plotting(
  control_var1 = 0,
  unit_var1 = "\u00B5M",
  endp_groups = list(group1 = c("BSL1","BSL2")),
  habituation_assay_list = c("ASH1"),
  reference_pos = -1,
  exp_name = "my_experiment",
  pathToExp = "/path/to/experiment"
)
}

}
