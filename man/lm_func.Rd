% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13_double_var_LM.R
\name{lm_func}
\alias{lm_func}
\title{Fit linear model and save results}
\usage{
lm_func(data, pathToExp)
}
\arguments{
\item{data}{Data frame containing columns `value`, `var1`, `var2`, `endpoint`, `chemical`, and `experiment`.}

\item{pathToExp}{Character string. Path to the experiment directory containing necessary subdirectories for input and output.}
}
\value{
A list of length two: the fitted `lm` model and the augmented data frame with predictions and metadata.
}
\description{
Fits a linear model to experimental data, generates predictions, and saves
model objects and augmented data. Includes an interaction with `var2` when
multiple `var2` levels are present.
}
\examples{
\dontrun{
sample_data <- data.frame(
  value = c(10, 15, 12, 18, 20, 25),
  var1 = c("A", "B", "A", "B", "A", "B"),
  var2 = c("X", "X", "Y", "Y", "Z", "Z"),
  endpoint = "growth",
  chemical = "compound1",
  experiment = "exp001"
)

results <- lm_func(sample_data, "/path/to/experiment")
summary(results[[1]])
head(results[[2]])
}

}
