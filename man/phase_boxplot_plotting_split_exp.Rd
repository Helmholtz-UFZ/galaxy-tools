% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_boxplotting_control_var1.R
\name{phase_boxplot_plotting_split_exp}
\alias{phase_boxplot_plotting_split_exp}
\title{Phase boxplot plotting (split experiments)}
\usage{
phase_boxplot_plotting_split_exp(
  phase_name,
  y_axis_title,
  pathToExp,
  unit_var1,
  var1_def,
  conc_data,
  control_var1
)
}
\arguments{
\item{phase_name}{Character string used in output filenames to identify the
endpoint set (e.g., "BSL1_VMR5" or "PhaseSums").}

\item{y_axis_title}{Character string for the y axis title.}

\item{pathToExp}{Character string. Path to the experiment directory containing necessary subdirectories for input and output.}

\item{unit_var1}{Character string. Unit of measurement for variable 1 (var1). If var1 does not have a unit, unit_var1 = NA.}

\item{var1_def}{Character string. Definition of the first variable that appears in the table next to the violin plot (e.g., if var1 is concentration, var1_def could be 'c', so that it appears as column name in the table as c('unit')).}

\item{conc_data}{Data frame that assigns colors to the groups of var1, var2, and experiment for each chemical.}

\item{control_var1}{Character/Numeric. Control value for var1}
}
\value{
No return value. Writes plots and statistical summaries to `pathToExp/result_plots/phase_boxplots` and `pathToExp/result_files/gam`.
}
\description{
Calculates and plots violin/boxplots for phase data per chemical and
experiment, and performs pairwise comparisons using fitted models.
}
\examples{
\dontrun{
phase_boxplot_plotting_split_exp(
  phase_name = "BSL1_VMR5",
  y_axis_title = "Summed distance moved (px/min)",
  pathToExp = "/path/to/experiment",
  unit_var1 = "\u00B5M",
  var1_def = "c",
  conc_data = conc_data
)
}

}
