% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_double_var_phase_data_calc_new.R
\name{phase_data_prep}
\alias{phase_data_prep}
\title{Prepare phase plotting data}
\usage{
phase_data_prep(endpoint_list, binsize, phase_name, exp_name, pathToExp)
}
\arguments{
\item{endpoint_list}{Character vector of endpoints to include.
For example: c("BSL1", "BSL2", "BSL3", "BSL4", "VMR1", "VMR2", "VMR3", "VMR4", "VMR5").}

\item{binsize}{Integer. Bin width in the time unit of `time` (e.g., seconds).
For example, `binsize = 60` calculates the sum over 60 time steps per bin.}

\item{phase_name}{Character string used in output filenames to identify the
endpoint set (e.g., "BSL1_VMR5" or "PhaseSums").}

\item{exp_name}{Character string specifying the experiment name, used in output file naming.}

\item{pathToExp}{Character string. Path to the experiment directory containing necessary subdirectories for input and output.}
}
\value{
No return value. Writes CSV and RDA files to `pathToExp/result_files/gam`.
}
\description{
Aggregates time series values (in the unit of the `time` column) into fixed size
bins for endpoints where time dependent behavior is important. For VAMR this
includes the visual motor responses and the baseline endpoints in the dark
phase at the beginning. Writes the combined dataset for downstream GAMM modeling.
}
\examples{
\dontrun{
phase_data_prep(
  endpoint_list = c("BSL1", "BSL2", "BSL3", "BSL4", "VMR1", "VMR2", "VMR3", "VMR4", "VMR5"),
  binsize = 60,
  phase_name = "BSL1_VMR5",
  exp_name = "my_experiment",
  pathToExp = "/path/to/experiment"
)
}

}
