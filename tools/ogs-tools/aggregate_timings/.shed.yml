name: Aggregate Timings
owner: ufz
description: Converts .json to .csv data.
long_description: |
    This tool searches through a specified directory for JSON files ending with `_total_timings.json`, extracts timing information, and writes the results to a CSV file.
categories:
- Convert Formats
remote_repository_url: https://github.com/Helmholtz-UFZ/galaxy-tools/tree/main/tools/ogs-tools
homepage_url: https://github.com/Helmholtz-UFZ/galaxy-tools/tree/main/tools/ogs-tools
type: unrestricted
auto_tool_repositories:
  name_template: "{{ tool_id }}"
  description_template: "Wrapper for a .json to .csv conveter: {{ tool_name }}."
suite:
  name: Aggregate Timings
  description: Converts .json to .csv data.
  long_description: |
      This tool searches through a specified directory for JSON files ending with `_total_timings.json`, extracts timing information, and writes the results to a CSV file.