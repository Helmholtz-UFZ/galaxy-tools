<tool name="OGS: GocadTSurfaceReader" id="ogs_gocadtsurfacereader" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="22.01">
  <description>Galaxy wrapper for the OGS utility 'GocadTSurfaceReader'.</description>
  <macros>
    <import>macros.xml</import>
    <import>test_macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <version_command><![CDATA[GocadTSurfaceReader --version]]></version_command>
  <command><![CDATA[GocadTSurfaceReader
    $write_binary
    $surfaces_only
    $lines_only
    --output-dir '$output_dir'
    --input-file '$input_file']]></command>
  <inputs>
    <param name="write_binary" type="boolean" label="write binary" help="if set, OGS-Meshes will be written in binary format" checked="false" truevalue="--write-binary" falsevalue=""/>
    <param name="surfaces_only" type="boolean" label="surfaces only" help="if set, only TSurf datasets will be parsed from the input file" checked="false" truevalue="--surfaces-only" falsevalue=""/>
    <param name="lines_only" type="boolean" label="lines only" help="if set, only PLine datasets will be parsed from the input file" checked="false" truevalue="--lines-only" falsevalue=""/>
    <param name="output_dir" type="text" optional="false" label="output dir" help="output directory"/>
    <param name="input_file" type="text" optional="false" label="input file" help="Gocad triangular surfaces file (*.ts). Provide a file with unix file endings under unix. Use dos2unix to convert."/>
  </inputs>
  <outputs/>
  <tests>
    <expand macro="gocadtsurfacereader_test"/>
  </tests>
  <help><![CDATA[This tool runs the **GocadTSurfaceReader** utility from the OpenGeoSys suite.]]></help>
  <expand macro="citations"/>
</tool>
