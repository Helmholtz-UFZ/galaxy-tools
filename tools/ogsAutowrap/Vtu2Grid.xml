<tool name="OGS: Vtu2Grid" id="ogs_vtu2grid" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="22.01">
  <description>Galaxy wrapper for the OGS utility 'Vtu2Grid'.</description>
  <macros>
    <import>macros.xml</import>
    <import>test_macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <version_command><![CDATA[Vtu2Grid --version]]></version_command>
  <command><![CDATA[Vtu2Grid
    #if str($cellsize_z):
        --cellsize-z '$cellsize_z'
    #end if
    #if str($cellsize_y):
        --cellsize-y '$cellsize_y'
    #end if
    --cellsize-x '$cellsize_x'
    --output '$output'
    --input '$input']]></command>
  <inputs>
    <param name="cellsize_z" type="float" value="1000" optional="true" label="cellsize z" help="edge length of cubes in z-direction (depth)"/>
    <param name="cellsize_y" type="float" value="1000" optional="true" label="cellsize y" help="edge length of cubes in y-direction (latitude)"/>
    <param name="cellsize_x" type="float" value="1000" optional="false" label="cellsize x" help="edge length of cubes in x-direction (longitude) or all directions, if y and z are not set"/>
    <param name="output" type="text" optional="false" label="output" help="the output grid (*.vtu)"/>
    <param name="input" type="text" optional="false" label="input" help="the 3D input mesh (*.vtu, *.msh)"/>
  </inputs>
  <outputs/>
  <tests>
    <expand macro="vtu2grid_test"/>
  </tests>
  <help><![CDATA[This tool runs the **Vtu2Grid** utility from the OpenGeoSys suite.]]></help>
  <expand macro="citations"/>
</tool>
