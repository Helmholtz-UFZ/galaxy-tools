<tool name="OGS: constructMeshesFromGeometry" id="ogs_constructmeshesfromgeometry" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="22.01">
  <description>Galaxy wrapper for the OGS utility 'constructMeshesFromGeometry'.</description>
  <macros>
    <import>macros.xml</import>
    <import>test_macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <version_command><![CDATA[constructMeshesFromGeometry --version]]></version_command>
  <command><![CDATA[constructMeshesFromGeometry
    #if str($searchlength):
        --searchlength '$searchlength'
    #end if
    --geometry '$geometry'
    --mesh '$mesh'
    $multiple_nodes_allowed]]></command>
  <inputs>
    <param name="searchlength" type="float" value="1e-16" optional="true" label="searchlength" help="search length determining radius for the node search algorithm. Non-negative floating point number, (min = 0)" min="0"/>
    <param name="geometry" type="data" optional="false" label="geometry" help="Input (.gml | .gli). The file the input geometry" format="gml,gli" multiple="false"/>
    <param name="mesh" type="data" optional="false" label="mesh" help="Input (.vtu). The file the input mesh where the geometry is defined" format="vtu" multiple="false"/>
    <param name="multiple_nodes_allowed" type="boolean" label="multiple nodes allowed" help="Allows multiple mesh nodes in eps environment." checked="false" truevalue="--multiple-nodes-allowed" falsevalue=""/>
  </inputs>
  <outputs/>
  <tests>
    <expand macro="ogsutilssuite_tests"/>
  </tests>
  <help><![CDATA[This tool runs the **constructMeshesFromGeometry** utility from the OpenGeoSys suite.]]></help>
  <expand macro="citations"/>
</tool>
