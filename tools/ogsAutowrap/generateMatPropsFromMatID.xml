<tool name="OGS: generateMatPropsFromMatID" id="ogs_generatematpropsfrommatid" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="22.01">
  <description>Galaxy wrapper for the OGS utility 'generateMatPropsFromMatID'.</description>
  <macros>
    <import>macros.xml</import>
    <import>test_macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <version_command><![CDATA[generateMatPropsFromMatID --version]]></version_command>
  <command><![CDATA[mkdir inputs; # Verzeichnis für saubere Trennung
#for $m in $mesh:
    ln -s "$m" "inputs/${m.element_identifier}";
#end for
generateMatPropsFromMatID
#for $m in $mesh:
    --mesh "inputs/${m.element_identifier}"
#end for]]></command>
  <inputs>
    <param name="mesh" type="data" optional="true" label="mesh" help="Input (.msh). The mesh to open from a file" format="xml" multiple="false"/>
  </inputs>
  <outputs/>
  <tests>
    <expand macro="generatematpropsfrommatid_test"/>
  </tests>
  <help><![CDATA[This tool runs the **generateMatPropsFromMatID** utility from the OpenGeoSys suite.]]></help>
  <expand macro="citations"/>
</tool>
