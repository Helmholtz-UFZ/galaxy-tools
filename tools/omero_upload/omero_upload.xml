<tool id="omero_upload" name="OMERO upload" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="20.01" license="MIT">
    <description></description>
    <!--<xrefs>
        <xref type="bio.tools"></xref>
    </xrefs>-->
    <macros>
        <token name="@TOOL_VERSION@">2.1.0</token>
        <token name="@VERSION_SUFFIX@">0</token>
    </macros>
    <!-- TODO: please annotate this tool with topics and operations from http://edamontology.org -->
    <!-- TODO: for more information see: https://galaxy-iuc-standards.readthedocs.io/en/latest/best_practices/tool_xml.html#edam-topics-and-operations -->
    <!--<edam_topics>
        <edam_topic>topic_TODO</edam_topic>
    </edam_topics>
    <edam_operations>
        <edam_operation>operation_TODO</edam_operation>
    </edam_operations>-->
    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">ezomero</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
        mkdir folder &&
        #for $image in $folder:
            ln -s '$image' folder/'$image.element_identifier'.$image.ext &&
        #end for


        omero import -f folder
            -s $omero_host
            -p $omero_port
            -u $__user__.extra_preferences.get('omero_account|username', "root")
            -w $__user__.extra_preferences.get('omero_account|password', "omero") 

        ## python '$__tool_directory__'/omero_upload.py
        ##     --url $omero_host
        ##     --port $omero_port
        ##     --user $__user__.extra_preferences.get('omero_account|username', "root")
        ##     --password $__user__.extra_preferences.get('omero_account|password', "omero")
        ##     --folder folder
    ]]></command>
    <inputs>
        <param name="omero_host" type="text" label="OMERO host URL">
            <validator type="regex" message="Enter a valid host location, for example, your.omero.server">^[a-zA-Z0-9._-]*$</validator>
            <validator type="expression" message="No two dots (..) allowed">'..' not in value</validator>
        </param>
        <param name="omero_port" type="integer" value="4064" optional="false" label="OMERO host URL"/>
        <param name="folder" type="data" format="bmp" multiple="true" optional="false" label="Images"/>
        <param name="username" type="hidden" value=""/>
        <param name="password" type="hidden" value=""/>
    </inputs>
    <outputs>
        <data name="log" format="txt"/>
    </outputs>
    <tests>
        <test>
            <param name="omero_host" value="localhost"/>
            <param name="omero_port" value="6064"/>
            <param name="folder" value="a1.bmp,b1.bmp"/>
            <param name="username" value="root"/>
            <param name="password" value="omero"/>
            <output name="log" value="test.log"/>
        </test>
        <!-- Hint: You can use [ctrl+alt+t] after defining the inputs/outputs to auto-scaffold some basic test cases. -->
    </tests>
    <help><![CDATA[

.. class:: infomark

**What it does**

TODO: Fill in help in reStructuredText format (https://docutils.sourceforge.io/docs/ref/rst/restructuredtext.html)

Usage
.....


**Input**


**Output**


    ]]></help>
    <citations>
        <!--<citation type="doi"> </citation>-->
    </citations>
</tool>